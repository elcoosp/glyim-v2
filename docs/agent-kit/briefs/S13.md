# Stream S13: MIR Definitions & Place Typing

## Mission
Implement Remove all `unimplemented!()` panics in `glyim-mir`, specifically in `Place::ty`. Implement `Place::ty` for ADT field projection, downcasts, derefs, and indexing using the `TypeLookup::field_ty` and `TypeLookup::adt_def` methods. Ensure `Body::dummy`, `Place::new`, `BasicBlockData::new`, `SwitchTargets`, and `from_raw`/`to_raw` conversions are fully unstubbed and functional. for the Glyim compiler.

## What You Own
- glyim-mir

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_mir::Body
- glyim_mir::Place

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-mir/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-mir/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S13-T01: Place::ty resolves through Deref and Field projections
- S13-T02: Place::ty resolves Downcast to correct variant type
- S13-T03: Body::dummy creates valid empty body
- S13-T04: SwitchTargets::iter yields correct branches

## Mocking Strategy
Use `glyim_test::assert_mir`.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-mir`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-mir` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-mir
cargo clippy -p glyim-mir -- -D warnings
cargo fmt --check -p glyim-mir
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S13/v0.1.0`

## Dependencies on Other Streams
- S12

## Downstream Consumers
- S17
- S18
- S19
- S20
- S21
