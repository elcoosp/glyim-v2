# Stream S13: AI Pilot – All Gates and Orchestrator

## Mission
Implement In `crates/glyim-pilot/src/gates/*.rs`, replace all `unimplemented!()` and `tracing::warn!("STUB: ...")` with full implementations for `ArchitectureGate`, `BannedPatternGate`, `ContractGate`, `CoverageGate`, `MutationGate`, `SelfReviewGate`, etc. In `orchestrator/turn.rs`, remove `tracing::warn!("STUB: drop glue scanning")` and implement commit decision logic. In `dispatch/rate_limit.rs`, implement backoff and failover. In `commit/engine.rs`, implement gate pipeline. for the Glyim compiler.

## What You Own
- glyim-pilot

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_pilot::gates::Gate::run
- glyim_pilot::orchestrator::process_turn_dispatch

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-pilot/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-pilot/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S13-T01: Architecture gate blocks forbidden dependencies
- S13-T02: Contract gate detects locked interface removal
- S13-T03: Coverage gate fails when coverage below threshold
- S13-T04: Rate limit handler escalates after max retries

## Mocking Strategy
Use mock `GateContext` and mock provider responses.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-pilot`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-pilot` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-pilot
cargo clippy -p glyim-pilot -- -D warnings
cargo fmt --check -p glyim-pilot
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S13/v0.1.0`

## Dependencies on Other Streams
- S10
- S11
- S12

## Downstream Consumers

