# Stream SV16: Core Crate (no_std)

## Mission
Implement Implement complete `core` library: `option`, `result`, `iter`, `slice`, `str`, `cell`, `mem`, `ptr`, `ops`, `cmp`, `marker`, `panic`, `hint`, `convert`, `default`. Include all lang items and panic handler. Must be compilable with Glyim and pass all tests. for the Glyim compiler.

## What You Own
- core

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- core::*

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/core/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/core/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- V16-T01: `Option::unwrap` works (run-pass)
- V16-T02: Iterate over slice using `for` loop (requires `IntoIterator`) (run-pass)
- V16-T03: `panic!` macro prints message and aborts (run-pass with expected abort)
- V16-T04: `mem::replace`, `mem::swap` (run-pass)
- V16-T05: `cmp::min`, `cmp::max` (run-pass)
- V16-T06: `Default` derive macro (run-pass)

## Mocking Strategy
Must compile with Glyim; no mocks. Use `glyim_test::TestRunner` with `--test` flag.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p core`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p core` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p core
cargo clippy -p core -- -D warnings
cargo fmt --check -p core
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-V16/v0.1.0`

## Dependencies on Other Streams
- V01
- V07
- V14

## Downstream Consumers
- V17
- V18
- V19
