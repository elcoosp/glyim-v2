# Stream SV28: LLVM Rvalue Lowering (Operators & Casts)

## Mission
Implement Lower remaining rvalues: `BinaryOp` (all integer, float, boolean, bitwise ops), `UnaryOp` (not, neg), `Cast` (int↔int, int↔float, pointer casts). Use LLVM instructions (add, sub, mul, udiv, sdiv, fadd, etc.) and handle overflow checks. for the Glyim compiler.

## What You Own
- glyim-codegen-llvm

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_mir::Rvalue
- glyim_type::Ty

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-codegen-llvm/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-codegen-llvm/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- V28-T01: Integer arithmetic (+, -, *, /, %) (run-pass)
- V28-T02: Comparisons (==, !=, <, >, <=, >=) (run-pass)
- V28-T03: Logical operators (&&, ||) via branches (run-pass)
- V28-T04: Bitwise ops (&, |, ^, <<, >>) (run-pass)
- V28-T05: Cast int to float and vice versa (run-pass)
- V28-T06: Cast pointer to integer and back (run-pass)
- V28-T07: Overflow checks for `+` in debug mode → panic (run-pass with abort)

## Mocking Strategy
Uses real types and values. Test with `lli`.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-codegen-llvm`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-codegen-llvm` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-codegen-llvm
cargo clippy -p glyim-codegen-llvm -- -D warnings
cargo fmt --check -p glyim-codegen-llvm
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-V28/v0.1.0`

## Dependencies on Other Streams
- V12

## Downstream Consumers
- V14
- V15
