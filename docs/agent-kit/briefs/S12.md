# Stream S12: Build Tool – Dependency Resolution and Fingerprinting

## Mission
Implement In `crates/glyip/src/dep.rs`, replace `tracing::warn!("STUB: registry fetch not implemented")` (line ~120) with actual HTTP index fetching and tarball download. In `fingerprint.rs`, replace `unimplemented!()` in `FingerprintStore::load_from_dir` and `save_to_dir` (lines ~50-70) with file‑based persistence. In `cache.rs`, replace `unimplemented!()` in `store_artifact` and `get_artifact` (lines ~80-90) with file caching. for the Glyim compiler.

## What You Own
- glyip

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyip::dep::DependencyResolver::resolve
- glyip::cache::Cache::needs_rebuild

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyip/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyip/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S12-T01: `glyip build` fetches dependency from registry
- S12-T02: Fingerprint detects file change
- S12-T03: Cached artifact reused when fingerprint unchanged
- S12-T04: `glyip test` runs test functions and reports results

## Mocking Strategy
Use temporary directories and mock HTTP server for registry.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyip`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyip` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyip
cargo clippy -p glyip -- -D warnings
cargo fmt --check -p glyip
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S12/v0.1.0`

## Dependencies on Other Streams


## Downstream Consumers

