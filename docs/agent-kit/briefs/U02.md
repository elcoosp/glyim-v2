# Stream SU02: MIR Lowering & Monomorphization

## Mission
Implement Remove stubs in `glyim-lower/src/lower.rs` (pattern destructuring, expr stmt dropped, unsupported literal, FnRef fallback, match guards, unhandled expr/place) and `glyim-pipeline/src/mono_cache.rs` (generic instantiation, drop glue generation, missing MIR body fallback). Also fix ADT field type resolution in `pipeline_context.rs`. for the Glyim compiler.

## What You Own
- glyim-lower
- glyim-pipeline

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:


## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-lower/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-lower/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- U02-T01: Let binding with tuple pattern destructures correctly (unit test)
- U02-T02: Expr statement with side effects is not dropped (unit test)
- U02-T03: Unsupported literal falls back to error constant (unit test)
- U02-T04: Match guard expression is correctly lowered (unit test)
- U02-T05: Generic function instantiation works (run-pass via pipeline)
- U02-T06: Drop glue generated for struct with Box field (run-pass)

## Mocking Strategy
Use `glyim_test::mock::MockLowerCtx` and `glyim_test::assert_mir`.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-lower`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-lower` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-lower
cargo clippy -p glyim-lower -- -D warnings
cargo fmt --check -p glyim-lower
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-U02/v0.1.0`

## Dependencies on Other Streams
- U01

## Downstream Consumers

