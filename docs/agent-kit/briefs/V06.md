# Stream SV06: Auto Traits (Send, Sync)

## Mission
Implement Automatically implement marker traits (Send, Sync, Unpin) for types when all field types implement the trait. Support negative impls (`impl !Send for T`) to opt out. Compute auto traits during type collection and store in `TyCtx`. Ensure coherence with manual impls. for the Glyim compiler.

## What You Own
- glyim-type
- glyim-typeck

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_type::TyFlags
- glyim_typeck::TypeckCtx

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-type/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-type/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- V06-T01: Struct with all Send fields → auto impl Send (run-pass)
- V06-T02: Struct with non‑Send field → no auto Send (compile-fail if used as Send)
- V06-T03: Negative impl `impl !Send for T` → overrides auto (run-pass)
- V06-T04: Transitive auto traits (Sync requires Send) (run-pass)
- V06-T05: Manual impl of auto trait overrides auto (run-pass)

## Mocking Strategy
Compute during type collection. Use `glyim_test::assert_ty` to check that `TyCtx::ty_flags` includes auto trait markers.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-type`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-type` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-type
cargo clippy -p glyim-type -- -D warnings
cargo fmt --check -p glyim-type
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-V06/v0.1.0`

## Dependencies on Other Streams
- V01
- V02

## Downstream Consumers
- V07
