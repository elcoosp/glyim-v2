# Stream S02: Foundation Core Unstubbing

## Mission
Implement Remove all `stub!()` and `stub_impl!()` calls in `glyim-core`. Implement `Interner::intern/resolve/lookup`, `Idx<T>` conversions, `define_idx` macro internals, `Path::from_single/as_name`, `validate_alignment`, and `TargetInfo` constructors. Ensure all primitive types, def-id structures, and arena allocations are fully operational. for the Glyim compiler.

## What You Own
- glyim-core

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_core::Interner
- glyim_core::Idx
- glyim_core::Path

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-core/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-core/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S02-T01: Interner correctly interns and resolves symbols
- S02-T02: define_idx macro generates valid IdxLike implementations
- S02-T03: Path::from_single and as_name work correctly
- S02-T04: validate_alignment accepts valid and rejects invalid alignments

## Mocking Strategy
No mocking. Direct unit testing of core data structures.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-core`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-core` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-core
cargo clippy -p glyim-core -- -D warnings
cargo fmt --check -p glyim-core
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S02/v0.1.0`

## Dependencies on Other Streams


## Downstream Consumers
- S04
- S05
- S06
- S08
- S09
- S10
