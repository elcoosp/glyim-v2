# Stream S02: Parser Unstubbing – All Missing Cases

## Mission
Implement Replace all `tracing::warn!("STUB: ...")` in `crates/glyim-frontend/src/parser/*.rs` with proper parsing logic. Specifically: `parse_token_tree` for macro repetition patterns (lines 220-250), `parse_type_arg_list` handling of `>>` (lines 520-540), `parse_where_clause` (lines 600-630), and `parse_visibility` for `pub(crate)` etc. Also implement missing `parse_label` (line 280-290) and `parse_loop_expr` (line 400-410). Remove all placeholder match arms that return `None` or `Err`. for the Glyim compiler.

## What You Own
- glyim-frontend

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_frontend::parser::Parser::parse_token_tree

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-frontend/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-frontend/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S02-T01: Macro with `$(...)*` parses correctly
- S02-T02: `pub(crate)` visibility parses and stores
- S02-T03: `where T: Clone + Send` parses as WhereClause
- S02-T04: `>>` in nested generic args parses as two `>` tokens

## Mocking Strategy
Snapshot testing via `glyim_test::snapshot_cst`.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-frontend`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-frontend` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-frontend
cargo clippy -p glyim-frontend -- -D warnings
cargo fmt --check -p glyim-frontend
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S02/v0.1.0`

## Dependencies on Other Streams


## Downstream Consumers

