# Stream S27: Test Infrastructure Unstubbing

## Mission
Implement Remove all `stub!()` and `stub_impl!()` calls in `glyim-test`. Update all mock types (`MockLowerCtx`, `MockSolver`, `MockBorrowckCtx`, `MockCodegen`) to match the extended traits (`field_index_by_name`, `variant_index_by_name`, `fn_sig`, `const_value`, `InferenceSnapshot` methods, etc.). Update phase testers to use new APIs. for the Glyim compiler.

## What You Own
- glyim-test

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_test::assert_ty
- glyim_test::snapshot_cst
- glyim_test::TestRunner

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-test/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-test/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S27-T01: MockLowerCtx implements field_index_by_name
- S27-T02: MockSolver implements InferenceSnapshot rollback
- S27-T03: Phase testers compile and run with updated APIs
- S27-T04: Existing snapshot tests pass

## Mocking Strategy
No mocking. The tests themselves are the verification.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-test`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-test` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-test
cargo clippy -p glyim-test -- -D warnings
cargo fmt --check -p glyim-test
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S27/v0.1.0`

## Dependencies on Other Streams
- S14
- S16
- S20

## Downstream Consumers

