# Stream S21: LSP – Incremental Analysis and Symbol Indexing

## Mission
Implement In `crates/glyim-lsp/src/driver.rs`, replace stub incremental compilation logic (line 60). Implement file change detection using file system watcher (notify) and re‑run only affected files. Maintain a dependency graph to rebuild only changed modules. In `symbol_index.rs`, replace stub `build_from_hir` (line 50) to traverse HIR and index all symbols (functions, structs, enums, fields, locals) with their spans and type signatures. Implement `lookup_by_name` and `lookup_by_location` efficiently (using hash maps). Add `query` for prefix search. Store symbol kind and documentation comments. for the Glyim compiler.

## What You Own
- glyim-lsp

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_lsp::AnalysisDriver::analyze_file
- glyim_lsp::SymbolIndex::build_from_hir

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-lsp/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-lsp/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S21-T01: Changing a function signature updates symbol index
- S21-T02: `lookup_by_name` returns correct symbol
- S21-T03: `query("foo")` returns all symbols starting with foo
- S21-T04: Renaming a symbol updates all references

## Mocking Strategy
Use `glyim_test::mock::TestDbBuilder` and simulate file changes.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-lsp`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-lsp` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-lsp
cargo clippy -p glyim-lsp -- -D warnings
cargo fmt --check -p glyim-lsp
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S21/v0.1.0`

## Dependencies on Other Streams
- S03
- S05

## Downstream Consumers
- S11
