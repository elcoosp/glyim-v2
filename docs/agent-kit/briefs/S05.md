# Stream S05: Type Checker – Method Resolution & Pattern Exhaustion

## Mission
Implement In `crates/glyim-typeck/src/check_expr.rs`, replace `tracing::warn!("STUB: method resolution fallback")` (line ~600) with proper trait‑based method lookup using `TraitSolver`. Also replace the stub for `Expr::For` (line ~350) with full desugaring to `IntoIterator`. In `check_pat.rs`, remove the `tracing::warn!("STUB: unsupported pattern")` and implement pattern checking for `Pat::Or`, `Pat::Range`, `Pat::Struct`. In `tyconv.rs`, replace `tracing::warn!("STUB: Cast PassMode")` (line ~200) with proper ABI handling. for the Glyim compiler.

## What You Own
- glyim-typeck

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_typeck::FnCtxt::check_expr
- glyim_typeck::tyconv::resolve_type_ref

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-typeck/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-typeck/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S05-T01: Method call `vec.push(1)` resolves to `Vec::push`
- S05-T02: `for x in iter` desugars to `IntoIterator` and type checks
- S05-T03: Pattern `Some(x) | None` is checked with correct types
- S05-T04: `Coerce` predicate works for `&mut T` to `&T`

## Mocking Strategy
Use `glyim_test::phase::AnalysisTester` with mock solver.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-typeck`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-typeck` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-typeck
cargo clippy -p glyim-typeck -- -D warnings
cargo fmt --check -p glyim-typeck
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S05/v0.1.0`

## Dependencies on Other Streams
- S04

## Downstream Consumers

