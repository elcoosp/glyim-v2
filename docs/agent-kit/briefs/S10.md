# Stream S10: Pipeline – Monomorphization and Drop Glue

## Mission
Implement In `crates/glyim-pipeline/src/mono_cache.rs`, replace `tracing::warn!("STUB: drop glue not found")` (line ~80) by implementing `generate_drop_glue` that walks the type fields and emits `Drop` terminators. In `pipeline_context.rs`, replace `tracing::warn!("ADT id not found")` (line ~60) by actually building ADT definition from HIR if missing. In `mono.rs`, replace `tracing::warn!("STUB: drop glue scanning")` by implementing `scan_terminator` for `Drop` to collect types that need drop glue. for the Glyim compiler.

## What You Own
- glyim-pipeline

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_pipeline::Pipeline::compile_file
- glyim_pipeline::mono_cache::generate_drop_glue

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-pipeline/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-pipeline/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S10-T01: Generic function `id<T>` monomorphized to `id<i32>` substitutes correctly
- S10-T02: Struct with `Drop` field generates drop glue that calls inner drop
- S10-T03: Enum drop glue switches on discriminant and drops active variant
- S10-T04: No `STUB` warnings emitted during pipeline execution

## Mocking Strategy
Use `glyim_test::phase::CompilationTrace`.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-pipeline`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-pipeline` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-pipeline
cargo clippy -p glyim-pipeline -- -D warnings
cargo fmt --check -p glyim-pipeline
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S10/v0.1.0`

## Dependencies on Other Streams
- S06
- S07
- S08
- S09

## Downstream Consumers

