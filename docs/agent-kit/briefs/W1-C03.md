# Stream W1-C03: MIR interpreter: all missing operations

## Mission
Implement Implement all missing interpreter functionality: bitwise (`&`, `|`, `^`, `<<`, `>>`), logical (`&&`, `||`), unary `Not` and `Neg`, `Len` (array/slice length), `Discriminant` (enum variant index), `SwitchInt` with >2 targets, `Aggregate` (tuples, arrays, ADTs), and nested projection writes (`write_through_projections_with_locals`). Remove all stubs. for the Glyim compiler.

## What You Own
- glyim-mir-interp

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_mir_interp::Interpreter::eval_rvalue
- glyim_mir_interp::Interpreter::eval_binary_op
- glyim_mir_interp::Interpreter::write_place

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-mir-interp/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-mir-interp/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- W1-C03-T01: `1 << 2` evaluates to 4
- W1-C03-T02: `!true` evaluates to false
- W1-C03-T03: `len([1,2,3])` returns 3
- W1-C03-T04: `match Some(5) { Some(x) => x, None => 0 }` returns 5
- W1-C03-T05: `(a, b).0 = 42` updates field

## Mocking Strategy
Construct MIR bodies programmatically and run interpreter.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-mir-interp`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-mir-interp` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-mir-interp
cargo clippy -p glyim-mir-interp -- -D warnings
cargo fmt --check -p glyim-mir-interp
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-W1-C03/v0.1.0`

## Dependencies on Other Streams


## Downstream Consumers

