# Stream W2-C02: Builtin macros: file!, line!, column!, env!, include!, concat!, stringify!

## Mission
Implement Implement all builtin macros in `expander/mod.rs`. Use `Span` information for `file!`, `line!`, `column!`. Use `std::env::var` for `env!`. Use `std::fs::read_to_string` for `include!`. Implement token concatenation for `concat!`. Implement `stringify!` to convert token stream to string literal. Add diagnostics for missing environment variables or files. for the Glyim compiler.

## What You Own
- glyim-meta

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_meta::Expander::expand
- glyim_meta::BuiltinMacro

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-meta/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-meta/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- W2-C02-T01: `file!()` expands to string literal with file name
- W2-C02-T02: `line!()` expands to integer literal with correct line number
- W2-C02-T03: `env!("PATH")` expands to string literal of PATH
- W2-C02-T04: `include!("test.txt")` expands to file content
- W2-C02-T05: `concat!("a", "b")` expands to `"ab"`
- W2-C02-T06: `stringify!(1+2)` expands to `"1 + 2"`

## Mocking Strategy
Mock `Span` with known byte offset; create temp files; set environment variables.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-meta`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-meta` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-meta
cargo clippy -p glyim-meta -- -D warnings
cargo fmt --check -p glyim-meta
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-W2-C02/v0.1.0`

## Dependencies on Other Streams


## Downstream Consumers

