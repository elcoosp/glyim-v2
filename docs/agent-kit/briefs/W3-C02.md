# Stream W3-C02: Coercions: array→slice, &mut→&, *mut→*const

## Mission
Implement Extend `can_coerce` in `fulfill.rs` to handle `&mut T → &T`, `&T → *const T`, `&mut T → *mut T`, `[T; N] → [T]`, and `*mut T → *const T`. Update `SimpleTraitSolver::evaluate_predicate` to return `SolverResult::Proven` for `Predicate::Coerce` when `can_coerce` returns true. for the Glyim compiler.

## What You Own
- glyim-solve

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_solve::fulfill::can_coerce
- glyim_solve::solver::SimpleTraitSolver::evaluate_predicate

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-solve/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-solve/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- W3-C02-T01: `&mut i32` coerces to `&i32`
- W3-C02-T02: `[1,2,3]` coerces to `&[i32]`
- W3-C02-T03: `*mut i32` coerces to `*const i32`
- W3-C02-T04: Coercion from `&i32` to `&mut i32` fails

## Mocking Strategy
Use `AnalysisTester` with function calls requiring coercions.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-solve`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-solve` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-solve
cargo clippy -p glyim-solve -- -D warnings
cargo fmt --check -p glyim-solve
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-W3-C02/v0.1.0`

## Dependencies on Other Streams


## Downstream Consumers
- W4-C06
