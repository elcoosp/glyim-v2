# Stream S03: HIR Lowering – Exhaustive Pattern & Expr Coverage

## Mission
Implement In `crates/glyim-hir/src/lower/lower_expr.rs`, replace the stub at lines 450-460 (`tracing::warn!("STUB: unhandled ExprKind")`) with exhaustive handling for `Expr::Closure`, `Expr::For`, `Expr::Range`, `Expr::Struct`, `Expr::Break`, and `Expr::Continue`. In `lower_pat.rs`, replace the stub at lines 320-330 (`tracing::warn!("STUB: unknown pattern kind")`) with full pattern lowering for `Pat::Or`, `Pat::Range`, `Pat::Struct` with `..`. Remove catch‑all `_ => Pat::Err` and `_ => Expr::Err`. for the Glyim compiler.

## What You Own
- glyim-hir

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_hir::lower::lower_expr
- glyim_hir::lower::lower_pat

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-hir/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-hir/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S03-T01: `for i in 0..10 { }` lowers to valid HIR For
- S03-T02: `Some(x) | None` pattern lowers to Pat::Or
- S03-T03: `1..=5` range expression lowers to Expr::Range inclusive
- S03-T04: `Point { x, y }` struct expr lowers with field names

## Mocking Strategy
Use `glyim_test::phase::FrontendTester` to get AST, then assert HIR structure.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-hir`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-hir` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-hir
cargo clippy -p glyim-hir -- -D warnings
cargo fmt --check -p glyim-hir
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S03/v0.1.0`

## Dependencies on Other Streams
- S02

## Downstream Consumers

