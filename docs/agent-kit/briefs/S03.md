# Stream S03: Language Library Verification

## Mission
Implement Verify and unstub `glyim-lang-core`, `glyim-lang-std`, and `glyim-lang-alloc`. Ensure `core_source()`, `core_modules()`, `core_source_all()` (and std/alloc equivalents) are fully implemented and return valid Glyim source strings for the compiler's core library injection. Replace any `stub!()` calls with static string returns. for the Glyim compiler.

## What You Own
- glyim-lang-core
- glyim-lang-std
- glyim-lang-alloc

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_lang_core::core_source
- glyim_lang_std::std_source
- glyim_lang_alloc::alloc_source

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-lang-core/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-lang-core/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S03-T01: core_source returns valid source for known modules
- S03-T02: std_source_all concatenates all std modules correctly
- S03-T03: alloc_modules lists expected modules

## Mocking Strategy
No mocking. String literal verification.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-lang-core`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-lang-core` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-lang-core
cargo clippy -p glyim-lang-core -- -D warnings
cargo fmt --check -p glyim-lang-core
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S03/v0.1.0`

## Dependencies on Other Streams


## Downstream Consumers

