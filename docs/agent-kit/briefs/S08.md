# Stream S08: Bytecode Backend – Projections and Constants

## Mission
Implement In `crates/glyim-codegen/src/lib.rs`, replace `tracing::warn!("STUB: Dummy layout provider")` (line ~60) with a real layout provider using `glyim-layout`. Implement `emit_place_address` for all projections (Deref, Field, Index) – currently only works for `Deref` (line ~80-120). Implement `emit_operand` for `String`, `Fn`, and `ConstRef` constant kinds (line ~200-220). Remove `tracing::warn!("STUB: PassMode Cast")` (line ~400) and implement ABI‑aware argument passing (indirect, cast, homogeneous aggregate). for the Glyim compiler.

## What You Own
- glyim-codegen

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_codegen::BytecodeBackend::emit_place_address
- glyim_codegen::BytecodeBackend::emit_operand

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-codegen/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-codegen/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S08-T01: Field projection emits correct offset using layout
- S08-T02: String constant emitted to string table
- S08-T03: Function constant emitted to function table
- S08-T04: Large struct passed indirectly via sret

## Mocking Strategy
Use `glyim_codegen::BytecodeBackend::generate_function` and inspect bytecode.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-codegen`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-codegen` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-codegen
cargo clippy -p glyim-codegen -- -D warnings
cargo fmt --check -p glyim-codegen
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S08/v0.1.0`

## Dependencies on Other Streams
- S06
- S07

## Downstream Consumers

