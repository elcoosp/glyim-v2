# Stream W5-C06: Runtime: UDP networking, threading, time

## Mission
Implement Implement UDP functions: `glyim_net_udp_bind`, `send_to`, `recv_from`, `connect`, `send`, `recv`. Implement threading: `glyim_thread_spawn`, `join`, `yield`, `sleep`, `park`, `unpark`, `current_id`, `available_parallelism`. Implement time: `glyim_time_now_secs`, `now_nanos`, `system_secs`, `system_nanos`. Use `std::net`, `std::thread`, `std::time`. Return negative error codes. for the Glyim compiler.

## What You Own
- glyim-runtime

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_runtime::glyim_net_udp_send_to
- glyim_runtime::glyim_thread_spawn
- glyim_runtime::glyim_time_now_secs

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-runtime/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-runtime/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- W5-C06-T01: UDP send/recv works
- W5-C06-T02: Thread spawn and join works
- W5-C06-T03: `glyim_time_now_secs` returns monotonic timestamp

## Mocking Strategy
Local ports, separate threads.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-runtime`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-runtime` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-runtime
cargo clippy -p glyim-runtime -- -D warnings
cargo fmt --check -p glyim-runtime
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-W5-C06/v0.1.0`

## Dependencies on Other Streams
- W1-C04

## Downstream Consumers

