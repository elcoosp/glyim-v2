# Stream S01: Soft-Stub Migration and Diagnostics

## Mission
Implement Modify the `stub!` macro in `glyim_diag/src/lib.rs` to expand to `unimplemented!()` instead of `compile_error!()`, and add a companion `stub_impl!` macro that also expands to `unimplemented!()`. Implement all `GlyimDiagnostic` constructors (`lex_error`, `parse_error`, `type_error`, `borrow_error`, `internal_error`), `with_source_code`, `with_sub`, `with_suggestion`, `DiagSink` methods (`emit`, `has_errors`, `into_diagnostics`), and `Display`/`Error` implementations. Remove all `stub!()` and `stub_impl!()` calls within `glyim-diag`, replacing them with functional diagnostic creation and reporting logic. for the Glyim compiler.

## What You Own
- glyim-diag

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_diag::stub!
- glyim_diag::GlyimDiagnostic
- glyim_diag::DiagSink

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-diag/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-diag/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S01-T01: Workspace compiles successfully after macro change
- S01-T02: Count of stub!() calls remains unchanged across workspace
- S01-T03: Calling a stubbed function at runtime panics with unimplemented!
- S01-T04: GlyimDiagnostic builders create correct severity and code
- S01-T05: DiagSink respects error limit and calls on_emit callback

## Mocking Strategy
No mocking. Direct macro definition change and structural testing.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-diag`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-diag` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-diag
cargo clippy -p glyim-diag -- -D warnings
cargo fmt --check -p glyim-diag
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S01/v0.1.0`

## Dependencies on Other Streams


## Downstream Consumers
- S04
- S05
- S06
- S08
