# Stream SV09: Two‑Phase Borrows

## Mission
Implement Support two‑phase borrows: reservation phase acts as shared, activation at call site. Implement `BorrowKind::Mut` with `allow_two_phase_borrow` flag and checking logic (reservation conflicts, activation point). for the Glyim compiler.

## What You Own
- glyim-borrowck
- glyim-mir

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_mir::BorrowKind::Mut
- glyim_borrowck::BorrowckCtx

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-borrowck/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-borrowck/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- V09-T01: `vec.push(vec.len())` compiles (run-pass)
- V09-T02: Two‑phase borrow with intervening shared borrow (run-pass)
- V09-T03: Activation point conflict detection → error (compile-fail)
- V09-T04: Reservation conflict with mutable borrow → error (compile-fail)

## Mocking Strategy
Extend borrowck with activation points. Use `glyim_test::assert_borrow_error`.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-borrowck`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-borrowck` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-borrowck
cargo clippy -p glyim-borrowck -- -D warnings
cargo fmt --check -p glyim-borrowck
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-V09/v0.1.0`

## Dependencies on Other Streams
- V08

## Downstream Consumers
- V11
