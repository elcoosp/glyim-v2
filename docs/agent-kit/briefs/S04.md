# Stream S04: Span & Hygiene Unstubbing

## Mission
Implement Remove all `stub!()` and `stub_impl!()` calls in `glyim-span`. Implement `SyntaxContext` operations, `ExpnId` logic, `HygieneCtx::push_expansion/apply_mark/remove_mark/adjust`, and `Span` methods (`is_dummy`, `range`, `sans_ctx`). Ensure `ByteIdx` and `FileId` conversions are fully functional and `From<Span> for miette::SourceSpan` works. for the Glyim compiler.

## What You Own
- glyim-span

## What You May NOT Modify
Any `pub` interface listed in `CONTRACTS_LOCKED.md` for crates you do NOT own.
Specifically, you MUST NOT change:
- glyim_span::Span
- glyim_span::HygieneCtx

## What You May Do Freely
- Add `pub(crate)` helper functions in crates you own
- Add new private modules in crates you own
- Add new `pub` items in NEW modules you create within crates you own
- Add dev-dependencies for testing

## Test Location (MANDATORY)

**ALL tests MUST be written as unit tests in `src/tests/` within the crate.**

NEVER use the `tests/` directory at the crate root (integration tests). Unit tests compile faster because they share the crate's private API.

### Setup Pattern

1. In `crates/glyim-span/src/lib.rs`, add:
   ```rust
   #[cfg(test)]
   mod tests;
   ```

2. Create `crates/glyim-span/src/tests/mod.rs` declaring submodules:
   ```rust
   mod module_a;
   mod module_b;
   ```

3. Each test file contains `#[test]` functions with full access to `pub(crate)` items.

4. Add `glyim-test` as a dev-dependency:
   ```toml
   [dev-dependencies]
   glyim-test = { workspace = true }
   ```

5. Read `docs/agent-kit/GLYIM_TEST_INSTRUCTIONS.md` for full API reference.

## Test Plan (Write These FIRST)
- S04-T01: Span::new creates valid span, is_dummy checks work
- S04-T02: HygieneCtx apply_mark/remove_mark roundtrip correctly
- S04-T03: ExpnId::ROOT and SyntaxContext::ROOT are valid defaults

## Mocking Strategy
No mocking. Direct structural testing.

## Implementation Priority
1. Create `src/tests/` module structure and wire up in `lib.rs`
2. Write ALL tests above — verify they compile with `cargo check -p glyim-span`
3. Implement until all tests pass
4. Remove all `todo!()` and silent stubs from non-test code
5. Run `cargo clippy -p glyim-span` — fix all warnings
6. Run `cargo fmt`

## Verification Commands
```bash
cargo test -p glyim-span
cargo clippy -p glyim-span -- -D warnings
cargo fmt --check -p glyim-span
cargo check --workspace
```

## Definition of Done
- [ ] All test cases pass
- [ ] Tests are in `src/tests/` (NOT `tests/`)
- [ ] Zero `todo!()` in non-test code
- [ ] Zero clippy warnings
- [ ] `cargo fmt` applied
- [ ] `cargo check --workspace` succeeds
- [ ] PR created against `main` with branch `stream-S04/v0.1.0`

## Dependencies on Other Streams
- S02

## Downstream Consumers
- S07
- S09
- S10
- S11
