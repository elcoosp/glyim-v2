Here’s your **human‑friendly launch plan** for the 33 v0.2.0 streams.  
Launch all streams in a **wave in parallel** (they have no mutual dependencies).  
**Wait** until every stream in the current wave is **finished and merged** before starting the next wave.

---

## Wave 1 – Foundation (launch all together)

| Stream | Name | Crate(s) |
|--------|------|----------|
| V01 | Associated Types & Projections | `glyim-type`, `glyim-solve`, `glyim-typeck` |
| V02 | Where Clauses & Supertraits | `glyim-hir`, `glyim-typeck`, `glyim-solve` |
| V08 | Polonius Region Inference (Base) | `glyim-borrowck` |
| V12 | LLVM Module & Basic Block Lowering | `glyim-codegen-llvm` |
| V22 | Mono Item Root Discovery | `glyim-lower`, `glyim-mir`, `glyim-type` |
| V23 | Mono Item Graph Traversal & Collection | `glyim-lower`, `glyim-mir`, `glyim-type` |

**These six streams can run simultaneously.**  
They only depend on the already‑completed v0.1.0 baseline, not on each other.

---

## Wave 2 – Types, Borrows & Rvalues (after Wave 1 is merged)

| Stream | Name | Crate(s) |
|--------|------|----------|
| V03 | Default Methods | `glyim-hir`, `glyim-typeck`, `glyim-lower` (depends on V01) |
| V04 | Coherence & Orphan Rules | `glyim-typeck`, `glyim-def-map` (depends on V01, V02) |
| V09 | Two‑Phase Borrows | `glyim-borrowck`, `glyim-mir` (depends on V08) |
| V13 | LLVM Rvalue Lowering (Aggregates & Memory Ops) | `glyim-codegen-llvm` (depends on V12) |
| V24 | Mono Item Cache & Deduplication | `glyim-lower` (depends on V23) |
| V25 | Codegen Unit Partitioning | `glyim-lower` (depends on V23, V24) |

**Launch all six together after Wave 1 is completely merged.**

---

## Wave 3 – Advanced Types & Move Analysis (after Wave 2 is merged)

| Stream | Name | Crate(s) |
|--------|------|----------|
| V05 | HRTB (Higher‑Ranked Trait Bounds) | `glyim-type`, `glyim-solve`, `glyim-typeck` (depends on V01) |
| V06 | Auto Traits (Send, Sync) | `glyim-type`, `glyim-typeck` (depends on V01, V02) |
| V10 | Move Analysis & Partial Moves | `glyim-borrowck`, `glyim-mir` (depends on V08) |
| V26 | Post‑Monomorphization Checks | `glyim-lower`, `glyim-typeck` (depends on V23) |
| V27 | LLVM ABI‑Aware Function Call Lowering | `glyim-codegen-llvm` (depends on V12, V13) |

**Launch all five together after Wave 2 is merged.**

---

## Wave 4 – Trait Objects, Drop Checking & Lowering Completion (after Wave 3 is merged)

| Stream | Name | Crate(s) |
|--------|------|----------|
| V07 | Trait Objects | `glyim-type`, `glyim-layout`, `glyim-codegen-llvm`, `glyim-codegen` (depends on V01, V03, V04, V06) |
| V11 | Drop Checking & Interior Mutability | `glyim-borrowck`, `glyim-type` (depends on V09, V10) |
| V14 | LLVM Function Calls & ABI | `glyim-codegen-llvm`, `glyim-layout` (depends on V13, V07) |
| V15 | LLVM Drop & Deallocation | `glyim-codegen-llvm`, `glyim-runtime` (depends on V13, V14) |
| V16 | Core Crate (no_std) | `core` (depends on V01, V07, V14) |
| V19 | Declarative Macros | `glyim-meta`, `glyim-frontend` (depends on V16) |
| V28 | LLVM Rvalue Lowering (Operators & Casts) | `glyim-codegen-llvm` (depends on V12) |
| V29 | LLVM SwitchInt & Switch Target Lowering | `glyim-codegen-llvm` (depends on V12) |
| V30 | LLVM Debug Info (DWARF) Generation | `glyim-codegen-llvm`, `glyim-span` (depends on V12–V15, V27–V29) |

**Launch all nine together after Wave 3 is merged.**  
(V07, V11, V14, V15, V16, V19, V28, V29, V30 can run in parallel – they touch different crates.)

---

## Wave 5 – Libraries & Mid‑Level Tools (after Wave 4 is merged)

| Stream | Name | Crate(s) |
|--------|------|----------|
| V17 | Alloc Crate | `alloc` (depends on V16, V15) |
| V18 | Std Crate (I/O, Files, Threads) | `std` (depends on V16, V17, V11, V14) |
| V20 | Build Tool (Glyip) | `glyip` (depends on V18, V19) |
| V31 | Polymorphization (Optional) | `glyim-lower`, `glyim-typeck` (depends on V23, V26) |

**Launch V17, V18, V20, V31 together after Wave 4 is merged.**  
(V31 is optional and can be skipped for v0.2.0.)

---

## Wave 6 – Final Polish & Integration (after Wave 5 is merged)

| Stream | Name | Crate(s) |
|--------|------|----------|
| V21 | Language Server Enhancements | `glyim-lsp` (depends on V18, V19) |
| V32 | LLVM Optimisation Pipeline Integration | `glyim-codegen-llvm` (depends on V12–V15, V27–V30) |
| V33 | End‑to‑End Monomorphization Pipeline Integration | `glyim-pipeline`, `glyim-db` (depends on V24, V25, V14, V15, V16) |

**Launch all three together after Wave 5 is merged.**

---

## Quick Summary

1. **Wave 1**: V01, V02, V08, V12, V22, V23 → 6 streams  
2. **Wave 2**: V03, V04, V09, V13, V24, V25 → 6 streams  
3. **Wave 3**: V05, V06, V10, V26, V27 → 5 streams  
4. **Wave 4**: V07, V11, V14, V15, V16, V19, V28, V29, V30 → 9 streams  
5. **Wave 5**: V17, V18, V20, V31 → 4 streams  
6. **Wave 6**: V21, V32, V33 → 3 streams  

**Critical rule:** Do not start a wave until **all** streams of the previous wave are **completed and merged** into `main`.  
Within a wave, you can assign each stream to a different agent/thread – they will not conflict because they modify different crates or are designed to be mergeable.
