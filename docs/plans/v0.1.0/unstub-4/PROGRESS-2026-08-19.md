# unstub-4 remaining-items implementation — progress (session 2026-08-19)

Status: COMPLETE & GREEN. `cargo build` + `cargo test` clean, 0 warnings, 3999 tests pass.

## Implemented this session
- §9.2 typeck `check_pat.rs` `check_pattern`: fixed-array (`[T;N]`) slice pattern with no `..` now errors if `prefix+suffix != N` (length mismatch diagnostic).
- §20.1 codegen `lib.rs` `emit_place_address` Downcast arm: adds `tag_offset` for direct-tagged enums. Added `LayoutProvider::tag_offset` (+ `TagEncoding`/`VariantsShape` import, impl on `GlyimLayoutProvider` in `impls.rs`). Regression test in `tests/downcast_projection.rs`.
- §20.2 codegen `lib.rs`: `Rvalue::Ref(place, _borrow_kind)` now binds the `BorrowKind` (documented that mutability is borrowck-enforced, not a distinct codegen opcode in this address-only backend).
- §22.4 lsp `diagnostics.rs`: added `ExternalDiagnostic`, `ExternalSeverity`, `parse_cargo_check_json` (cargo `--message-format=json` parser), `convert_external_diagnostics`. Unit tests added.
- §19.4 codegen-llvm `passes.rs`: `run_llvm_passes_with(module, tm, opt, size, custom: Option<&str>)` overrides the built-in `default<Ox>` pipeline; `run_llvm_passes` delegates. Test added.

## Verified ALREADY DONE in code (plan doc was stale) — no change needed
- §11.1 mir `mir/lib.rs::ty_mut` already returns `[T; len]` for array subslices (test `ty_mut_subslice_on_array_returns_array_type`). The read-only `ty` keeps its per-mission fallback. Attempted edit to `ty` reverted.
- §12.1 `lower/builder.rs lower_closure`: capture analysis (Tier 1.1) + captures passed as args already present. Only synthetic `DefId` (structural single-crate-model artifact) remains.
- §2.1 hygiene `adjust` present in `glyim-span/src/hygiene.rs`.
- §21.1–21.3 macro expanders (Env, OptionEnv, IncludeStr, IncludeBytes, Include, Concat, ConcatIdents) all present in `glyim-meta/src/expander/mod.rs`.
- §22.3 workspace_symbols uses `symbol_index.query` (fuzzy/prefix).
- §22.6 completion auto-import implemented (`completion.rs`).
- §23.3 fingerprint `has_any_changed`/`update_all` take `extension` param (configurable, not hardcoded `.g`).
- §23.5 `cache.rs global_cache_dir` implemented.
- §24.1 `executor.run_parallel` progress via channel; §24.3 `snapshot_mir` includes `var_debug_info`.

## Note
- `glyim-test` crate had 1 transient parallel-test flake (`test_program_runner_echo`) during one full-suite run; green in isolation and on re-run. Not a regression from these changes.
- All edits are uncommitted in the working tree (branch main).
