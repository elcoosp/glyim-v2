# unstub-5 Progress — 2026-08-20

Goal (verbatim): implement everything in `unstub-5/README.md`, green, committed,
and pushed; do not ask input until done.

## Status as of 2026-08-20
- P1 Generics: DONE + committed + pushed (origin/main HEAD 9c9a953).
- P2 Trait core / P3 where-clauses: verified already implemented; no new work.
- P4 extern ABI/FFI: DONE + committed + pushed (ed316d1).
- P8 LSP Completeness: DONE in code + committed (see below), 69 LSP tests pass,
  full workspace green (0 failures, 0 warnings).
- P5/P6/P7/P9/P10: NOT started (research-grade; tracked in KNOWN_GAPS.md).

## RESTART INSTRUCTIONS
1. `git log --oneline -8` — confirm HEAD. At session end expect Phase 8 commits
   on top of ed316d1.
2. `cargo test` must be green (0 failures, 0 warnings) before any new phase.
3. Read `KNOWN_GAPS.md` (same dir) for the deferred items and the exact macro-
   lowering gap that currently forces the rename text-fallback to stay.
4. Next phase to attempt: P5 (async/.await) — start by reading
   `README.md` §5 (`## Phase 5`). Keep atomically-committed, push after each
   green phase.

## Phase 8 commit plan (atomic)
- `crates/glyim-lsp/src/reference_graph.rs`: exhaustive `walk_expr` (8.1).
- `crates/glyim-lsp/src/rename.rs`: graph-primary rename, fallback retained.
- `crates/glyim-lsp/src/tests/{graph_tests,reference_graph_tests,rename_tests}.rs`:
  tests + stale-comment cleanup.
- `docs/plans/v0.1.0/unstub-5/KNOWN_GAPS.md`: gap tracking.
