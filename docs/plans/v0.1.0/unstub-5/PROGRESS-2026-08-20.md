# unstub-5 Progress — 2026-08-20 (updated)

Goal (verbatim): implement everything in `unstub-5/README.md`, green, committed,
and pushed; do not ask input until done. Windows-specific phases verified via
GitHub Actions CI (the user's standing directive).

## Status as of 2026-08-20 (session 2)
- P1 Generics: DONE + pushed (9c9a953).
- P2/P3: already implemented; no new work.
- P4 extern ABI/FFI: DONE + pushed (ed316d1).
- P8 LSP: DONE + pushed.
- P9.1 concat_idents metadata: DONE + pushed (db56293). True token hygiene deferred (KNOWN_GAPS §9.1).
- **CI (.github/workflows/ci.yml): DONE — all three jobs GREEN**
  (ubuntu via llvm.sh+libpolly-22-dev, macos via brew llvm@22,
  windows via glyim-runtime-only nextest). Verified run 32290926561 success.
- **P6.4 Windows graceful process signaling: DONE + verified on Windows CI.**
  - Added `windows-sys` 0.59 dep to glyim-runtime.
  - `glyim_process_kill` cfg(not(unix)) now branches: SIGTERM(15)/SIGINT(2)
    -> GenerateConsoleCtrlEvent (graceful, CTRL_BREAK/CTRL_C); other -> TerminateProcess (hard).
  - Spawn sets CREATE_NEW_PROCESS_GROUP on Windows so graceful signaling has a group to target.
  - Fixed pre-existing Windows build/test bugs that CI surfaced: fs.rs path_from_raw
    -> PathBuf (lifetimes), lib.rs OpenProcess null-check (.is_null()), and
    gated Unix-only tests (getppid, printf-spaces) + tolerating unset HOME on Windows.
  - Also fixed cross-platform type bug: glyim_thread_current_id casts pthread_self() as usize.
- P5/P6.1/P6.2-closures/P7/P9.2/P10: NOT started (research-grade; tracked in KNOWN_GAPS.md).

## RESTART INSTRUCTIONS
1. `git log --oneline -5` — confirm HEAD (expect recent P6.4/CI commits; HEAD ~f2e2908).
2. `cargo nextest run --workspace` must be green before any new phase. Use **nextest**.
3. CI is the source of truth for Windows. Push and watch the run:
   `gh run list --limit 1` then `gh run watch <id>`.
   Windows job only builds+runs `glyim-runtime` (no LLVM needed) — sufficient to
   verify P6.4. Full workspace (LLVM) runs on ubuntu+macos.
4. Next phase to attempt: **P6.1 SEH unwinding on Windows** (funclet FFI
   wrappers + a cross-compile-and-inspect-IR test, since the Windows job does
   not build codegen-llvm). Then P6.2 closures debug-info, P10, P5, P7, P9.2.
5. Pre-existing gap (not my phases): native standalone compiled-exec test
   `compile_and_run_compiled_runs_real_binary` in glyip is `#[ignore]`d because
   glyim codegen does not emit a standalone `main`/start symbol for a bare `cc`
   link. Not part of unstub-5 phasing.

## Windows CI gotchas already solved (don't re-litigate)
- mise has NO llvm@22 registry entry -> use brew (macos) / llvm.sh+libpolly-22-dev (ubuntu).
- ubuntu llvm-sys needs `libpolly-22-dev` or Polly link fails.
- glyim-runtime TEST target drags LLVM via glyim-test dev-dep; removed that
  dev-dep so Windows runtime tests build LLVM-free.
- CI file: `.github/workflows/ci.yml`.
