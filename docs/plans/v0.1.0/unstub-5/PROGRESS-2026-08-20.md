# unstub-5 Progress — 2026-08-20

Goal (verbatim): implement everything in `unstub-5/README.md`, green, committed,
and pushed; do not ask input until done.

## Status as of 2026-08-20
- P1 Generics: DONE + committed + pushed (origin/main HEAD 9c9a953).
- P2 Trait core / P3 where-clauses: verified already implemented; no new work.
- P4 extern ABI/FFI: DONE + committed + pushed (ed316d1).
- P8 LSP Completeness: DONE in code + committed (see below), 69 LSP tests pass,
  full workspace green (0 failures, 0 warnings).
- P9.1 `concat_idents!` expansion metadata: DONE + committed (db56293).
  True token-level hygiene is DEFERRED (see KNOWN_GAPS.md §9.1) — the
  synthesized identifier is not yet kept distinct per-expansion at resolution
  time because `apply_mark` is never invoked and `TokenTree::Token` carries no
  SyntaxContext. This is a real, documented gap, not complete.
- P5/P6/P7/P9.2/P10: NOT started (research-grade; tracked in KNOWN_GAPS.md).

## RESTART INSTRUCTIONS
1. `git log --oneline -8` — confirm HEAD. At session end expect Phase 8 + 9.1
   commits on top of ed316d1.
2. `cargo nextest run --workspace` must be green (0 failures, 0 warnings)
   before any new phase. Use **nextest**, not `cargo test` (user directive).
3. Read `KNOWN_GAPS.md` (same dir) for the deferred items: the macro-lowering
   gap forcing the rename text-fallback to stay, and the true `concat_idents!`
   token-hygiene gap (apply_mark never invoked).
4. Next phase to attempt: P5 (async/.await) — start by reading
   `README.md` §5 (`## Phase 5`). Keep atomically-committed, push after each
   green phase. Avoid breaking the green workspace (e.g. lowering macros to
   `Call` ripples into typeck/codegen — verify with nextest).

## Phase 8 commit plan (atomic)
- `crates/glyim-lsp/src/reference_graph.rs`: exhaustive `walk_expr` (8.1).
- `crates/glyim-lsp/src/rename.rs`: graph-primary rename, fallback retained.
- `crates/glyim-lsp/src/tests/{graph_tests,reference_graph_tests,rename_tests}.rs`:
  tests + stale-comment cleanup.
- `docs/plans/v0.1.0/unstub-5/KNOWN_GAPS.md`: gap tracking.
